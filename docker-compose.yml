services:
  zotero-private-search:
    build: .
    ports:
      - "127.0.0.1:8765:8765"
    volumes:
      - ~/Zotero:/zotero:ro
      - chroma-data:/data/chroma
    cap_add:
      - NET_ADMIN
    restart: unless-stopped

volumes:
  chroma-data:
