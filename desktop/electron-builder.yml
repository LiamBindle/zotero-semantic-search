appId: io.github.liambindle.zotero-private-search
productName: Zotero Private Search

directories:
  output: dist

files:
  - src/**/*
  - package.json

mac:
  target:
    - target: dmg
      arch: [universal]
  category: public.app-category.productivity

win:
  target:
    - target: nsis
      arch: [x64]

nsis:
  oneClick: true
  perMachine: false
  createDesktopShortcut: true
  createStartMenuShortcut: true

linux:
  target:
    - target: AppImage
      arch: [x64]
  category: Science

publish:
  provider: github
  owner: LiamBindle
  repo: zotero-private-search
